### Database:

- Docker command:

```
docker run --name my-postgres -e POSTGRES_PASSWORD=password -p 5432:5432 -d postgres:18
```

- For logging in: Username - `user`, Password - `Using generated security password: ` in terminal
